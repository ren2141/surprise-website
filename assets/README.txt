Optional catch-all folder — not read by the site itself.

Handy for things like:
- The QR code image once you've generated one for the deployed link
- Any extra source files (original high-res photos, a font you
  chose to self-host, etc.)

Nothing here needs to exist for the site to work.
